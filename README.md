# Basket KGCHO Landing

**Легкий и удобный лендинг** для турнира КГЧО по классическому волейболу.

Современный статический сайт на базе Astro с красивым дизайном и быстрой загрузкой.

---

## 🚀 Быстрый старт

### Установка

```bash
pnpm install
```

### Запуск

```bash
pnpm dev:web
```

Сайт будет доступен по адресу: **http://localhost:5100**

### Сборка для продакшена

```bash
pnpm build:web
```

Готовый статический сайт появится в папке `apps/web/dist/`

---

## 🛠️ Технологии

- **Astro 5.11.0** — быстрый статический генератор
- **Tailwind CSS 4.1.11** — современная стилизация
- **TypeScript** — типизация
- **React 19** — для интерактивных компонентов

---

## 📁 Структура проекта

```
/apps/web
├── /src
│   ├── /components/tournament  # Компоненты турнира
│   ├── /layouts               # Layout компоненты
│   ├── /pages                 # Страницы
│   └── /assets                # Стили и ресурсы
└── /public                    # Статические файлы (изображения, SVG)
```

---

## 📤 Деплой на Linux сервер (VPS)

Для этого проекта настроен адаптер `@astrojs/node` для работы на обычном сервере с Node.js.

### 1. Подготовка на GitHub
1. Создайте репозиторий на GitHub.
2. Сделайте `push` вашего кода в этот репозиторий.
3. Создайте **GitHub App** в настройках профиля (Developer settings) для работы админки Keystatic:
   - Homepage URL: `https://vash-sait.ru`
   - Callback URL: `https://vash-sait.ru/api/keystatic/github/callback`
   - Permissions: Repository -> Contents (Read & Write), Pull Requests (Read & Write).

### 2. Деплой на сервер (Fastpanel/SSH)
В корне проекта есть скрипт `deploy.sh`. Вы можете использовать его или выполнить команды вручную:

```bash
# Клонируйте репозиторий
git clone https://github.com/vash-akk/repo-name.git
cd repo-name

# Установите зависимости и соберите проект
pnpm install
pnpm build:web

# Запустите сервер через PM2
pm2 start apps/web/dist/server/entry.mjs --name basket-site
```

### 3. Переменные окружения (.env)
На сервере создайте файл `.env` в папке `apps/web/` со следующими ключами от вашего GitHub App:
- `KEYSTATIC_GITHUB_CLIENT_ID`
- `KEYSTATIC_GITHUB_CLIENT_SECRET`
- `KEYSTATIC_SECRET` (произвольная длинная строка)

---

## 🎨 Особенности

- ⚡ Быстрая загрузка (статический HTML/CSS/JS)
- 📱 Адаптивный дизайн
- 🎯 Современный UI/UX
- 🚀 Оптимизированные изображения
- 💨 Минимальный размер бандла

---

**Built with ❤️ for KGCHO Tournament**
