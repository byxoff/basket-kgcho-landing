# My Peak Ecosystem v2026

**DeepTech-платформа спортивной аналитики** с фокусом на производительность и GEO (Generative Engine Optimization).

---

## 🎨 Темы и Frontend

Мы используем систему сменных тем для Astro.

### Текущая тема: **Blackspike**
Расположение: `packages/themes/blackspike`
Симлинк: `apps/web` -> `packages/themes/blackspike`

### Как менять темы (как перчатки 🧤)

**Автоматический способ (рекомендуется):**
```bash
./scripts/switch-theme.sh blackspike
# или
./scripts/switch-theme.sh minimal
```

**Ручной способ:**
1. **Создай новую тему** в `packages/themes/my-new-theme`
2. **Скопируй тему в apps/web**:
   ```bash
   rm -rf apps/web
   cp -r packages/themes/my-new-theme apps/web
   rm -rf apps/web/.git
   
   # Обнови package.json (имя должно быть "web")
   # Обнови dev скрипт на порт 5000
   
   # Переустанови зависимости
   pnpm install
   ```

---

## 🏗️ Архитектура

Проект организован как **Monorepo** с использованием:
- **pnpm Workspaces** — управление зависимостями
- **Turborepo** — оркестрация билдов и задач
- **Tailwind CSS v4** — единая дизайн-система

---

## 📁 Структура проекта

```
/my-peak-landing
├── /apps
│   ├── /web -> ../packages/themes/blackspike  # Симлинк на активную тему
│   └── /core                                  # Next.js 15 - SaaS Dashboard (порт 5001)
├── /apps-cms                                  # Payload CMS 3.0 - Админка и API
├── /packages
│   ├── /ui                                    # Общая дизайн-система
│   └── /themes                                # Папка с темами для лендинга
│       ├── /minimal                           # Старая минималистичная тема
│       └── /blackspike                        # Активная тема (Blackspike)
├── pnpm-workspace.yaml
├── turbo.json
└── package.json
```

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pnpm install
```

### Запуск всех сервисов

```bash
# Запуск web + core через Turborepo
pnpm dev

# Запуск всех сервисов (web + core + cms)
pnpm dev-all
```

### Запуск отдельных сервисов

```bash
# Только Astro лендинг (порт 5000)
pnpm dev:web

# Только Next.js Dashboard (порт 5001)
pnpm dev:core

# Только Payload CMS (порт 3000 по умолчанию)
pnpm dev:cms
```

---

## 📦 Приложения

### 1. **apps/web** (Astro Theme)

**Технологии:**
- Astro 5.0
- Tailwind CSS v4
- Swiper (в теме Blackspike)

**Порт:** `5000`  
**URL:** http://localhost:5000

**Команды:**
```bash
pnpm dev:web
```

---

### 2. **apps/core** — Next.js 15 (SaaS Dashboard)

**Технологии:**
- Next.js 15 (App Router)
- React 19
- Tailwind CSS v4
- TypeScript

**Порт:** `5001`  
**URL:** http://localhost:5001

**Команды:**
```bash
pnpm dev:core
```

---

### 3. **apps-cms** — Payload CMS 3.0 (Админка и API)

**Технологии:**
- Payload CMS 3.0
- Next.js 15
- PostgreSQL

**Порт:** `3000` (по умолчанию)  
**URL:** http://localhost:3000/admin

**Команды:**
```bash
pnpm dev:cms
```

---

## 🔧 Конфигурация

### Порты

- **Astro (web):** `5000`
- **Next.js (core):** `5001`
- **Payload CMS:** `3000` (по умолчанию)

---

## 📤 Деплой

### SFTP деплой (Reg.ru)

Конфигурация находится в `.vscode/sftp.json`:
- **Хост:** `31.31.197.38`
- **Путь:** `/www/mypeak.ru`
- **Контекст:** `./apps/web/dist`

**Использование:**
1. Собери проект: `pnpm build:web`
2. Используй расширение SFTP в VS Code для загрузки `dist/` на сервер

---

**Built with ❤️ for My Peak Team**
