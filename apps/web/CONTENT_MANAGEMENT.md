# 📖 Управление контентом сайта турнира КГЧО

## Принцип: Content is Data

Мы не пишем текст внутри компонентов. Весь контент хранится отдельно в папке `src/content/` в формате **JSON**, а дизайн — в компонентах.

**Формат:** Все файлы контента — это JSON (`index.json`).

---

## 🚀 Быстрый старт

### Вариант 1: Через Keystatic (UI как в WordPress)

1. Запусти dev-сервер:
   ```bash
   pnpm dev
   ```

2. Открой в браузере:
   ```
   http://localhost:5100/keystatic
   ```

3. Видишь красивую админку с полями, кнопками, загрузкой картинок — всё как в WordPress, но без базы данных.

4. Меняешь контент → жмёшь "Save" → Keystatic сам обновляет JSON файлы в `src/content/` и делает коммит в Git.

**Это уровень God-Tier.** Ты даёшь им админку, а по факту они редактируют JSON файлы. Всё прозрачно, всё в Git, никаких баз данных.

---

### Вариант 2: Прямое редактирование файлов (Хардкор)

Если не боишься кода, открывай JSON файлы в `src/content/` и прави напрямую:

- `src/content/schedule/index.json` — расписание турнира
- `src/content/shop/index.json` — товары магазина
- `src/content/partners/index.json` — партнёры
- `src/content/tournament-settings/index.json` — основные настройки (заголовок, дата, место)

**Важно:** Используй JSON файлы (`.json`), не YAML. Компоненты читают только JSON.

---

## 📁 Структура контента

Все файлы контента находятся в `src/content/` в формате **JSON**. Компоненты читают данные напрямую из этих файлов.

### Расписание (`src/content/schedule/index.json`)

```json
{
  "day1": [
    { "time": "10:00", "event": "Церемония открытия" },
    { "time": "10:30", "event": "Групповой этап - Мужчины" }
  ],
  "day2": [
    { "time": "10:00", "event": "Четвертьфинал - Мужчины (1/4)" }
  ]
}
```

**Как править:** Добавляй/удаляй события в массивы `day1` и `day2`, меняй время и названия.

---

### Магазин (`src/content/shop/index.json`)

```json
{
  "items": [
    {
      "title": "Официальный мяч Турнира",
      "description": "Профессиональный мяч для игры в баскетбол 3x3",
      "price": "1490 ₽",
      "image": "/tournament/balls_2.png",
      "link": "#shop"
    }
  ]
}
```

**Как править:** Добавляй новые объекты в массив `items`, меняй цены, описания, картинки.

---

### Партнёры (`src/content/partners/index.json`)

```json
{
  "items": [
    {
      "image": "/path/to/logo.png",
      "link": "https://partner-site.ru",
      "name": "Название партнёра"
    }
  ]
}
```

**Как править:** Добавляй новые объекты в массив `items` с логотипами партнёров и ссылками.

---

### Настройки турнира (`src/content/tournament-settings/index.json`)

```json
{
  "title": "МЕЖДУНАРОДНЫЙ ТУРНИР ПО БАСКЕТБОЛУ 3Х3",
  "subtitle": "НА КУБОК ГУБЕРНАТОРА ЧЕЛЯБИНСКОЙ ОБЛАСТИ",
  "date": "13-14 марта 2026",
  "location": "ДС «Юность»",
  "logo": "/tournament/logo-big.png",
  "heroBg": "/tournament/hero-bg.png"
}
```

**Как править:** Меняй значения полей — заголовки, даты, место проведения, пути к картинкам.

---

## 🖼️ Картинки

**Где хранить:** В папке `src/assets/tournament/` или `public/tournament/`

**Важно:** 
- Astro автоматически оптимизирует изображения из `src/assets/` (конвертирует в AVIF, сжимает)
- Изображения из `public/` не оптимизируются, но доступны по прямым путям

**Рекомендация:** Используй `src/assets/` для новых изображений.

---

## ⚠️ Безопасность (Валидация)

Если при запуске вылезла ошибка:
```
Validation Error: "title" is required
```

**Это значит:** Ты забыл заполнить обязательное поле. Читай ошибку — она друг. Исправь и перезапусти.

---

## 🎨 Что НЕ трогать

**Не лезь в папку `src/components/`**, если не хочешь сломать верстку. Там живёт дизайн.

**Можно править:**
- ✅ `src/content/` — весь контент
- ✅ `src/assets/` — картинки
- ✅ `public/` — статические файлы

**Нельзя править без понимания:**
- ❌ `src/components/` — компоненты (верстка)
- ❌ `src/layouts/` — шаблоны страниц
- ❌ `astro.config.mjs` — конфигурация

---

## 🔧 MDX компоненты (для продвинутых)

Если пишешь статьи в MDX, можешь использовать компоненты прямо в тексте:

```mdx
# Заголовок статьи

<Chart type="heart-rate" value={140} color="red" />

<BentoCard title="Инсайт недели" icon="⚡️">
  Текст карточки
</BentoCard>
```

Доступные компоненты:
- `<Chart>` — графики и метрики
- `<BentoCard>` — карточки с иконками

---

## 📝 Примеры

### Добавить новое событие в расписание

Открой `src/content/schedule/index.json` и добавь новый объект в массив `day1` или `day2`:

```json
{
  "day1": [
    { "time": "10:00", "event": "Церемония открытия" },
    { "time": "21:00", "event": "Новое событие" }
  ]
}
```

**Важно:** Следи за запятыми между объектами и правильными фигурными скобками. Сохрани → сайт обновится автоматически.

---

### Добавить товар в магазин

Открой `src/content/shop/index.json` и добавь новый объект в массив `items`:

```json
{
  "items": [
    {
      "title": "Официальный мяч Турнира",
      "description": "Профессиональный мяч для игры в баскетбол 3x3",
      "price": "1490 ₽",
      "image": "/tournament/balls_2.png",
      "link": "#shop"
    },
    {
      "title": "Новый товар",
      "description": "Описание товара",
      "price": "2000 ₽",
      "image": "/tournament/new-product.png",
      "link": "#shop"
    }
  ]
}
```

**Важно:** Не забудь запятую после предыдущего объекта перед добавлением нового.

---

## 🚨 Troubleshooting

**Сайт не собирается:**
- Проверь синтаксис JSON (запятые, фигурные скобки, кавычки)
- Используй валидатор JSON (например, [jsonlint.com](https://jsonlint.com/))
- Посмотри ошибки в терминале — они укажут на проблемную строку
- Убедись, что все обязательные поля заполнены

**Частые ошибки JSON:**
- Лишняя или пропущенная запятая в конце массива/объекта
- Незакрытые фигурные или квадратные скобки
- Кавычки должны быть двойными `"`, не одинарными `'`

**Изменения не отображаются:**
- Перезапусти dev-сервер (`Ctrl+C`, потом `pnpm dev`)
- Очисти кэш браузера (`Cmd+Shift+R` на Mac, `Ctrl+Shift+R` на Windows)

**Keystatic не открывается:**
- Убедись, что dev-сервер запущен
- Проверь, что порт 5100 свободен
- Посмотри ошибки в терминале

---

## 💡 Советы

1. **Делай коммиты часто** — каждый раз после изменения контента
2. **Тестируй локально** — всегда проверяй изменения на `localhost:5100` перед пушем
3. **Используй Keystatic** — если команда не знает JSON/YAML, дай им админку через `/keystatic`
4. **Валидируй JSON** — перед сохранением проверь синтаксис через валидатор
5. **Оптимизируй картинки** — не загружай PNG весом в 5 МБ, Astro поправит, но зачем нагружать билд
6. **Используй форматирование** — большинство редакторов умеют форматировать JSON (`Shift+Alt+F` в VS Code)

## 🔄 Как это работает

1. **Keystatic** редактирует JSON файлы через UI → сохраняет в `src/content/*/index.json`
2. **Компоненты** импортируют JSON напрямую: `import data from '../../content/.../index.json'`
3. **Astro** компилирует всё в статический сайт

**Никаких баз данных, никаких API — всё в файлах, всё в Git.**

---

---

## 📚 Полезные ссылки

- [Валидатор JSON](https://jsonlint.com/) — проверь синтаксис перед сохранением
- [JSON форматирование](https://jsonformatter.org/) — красивое форматирование JSON
- [Keystatic документация](https://keystatic.com/docs) — если хочешь настроить что-то дополнительно

---

**Вопросы?** Читай ошибки в терминале — они всегда подскажут, что не так. Если что-то сломалось — проверь синтаксис JSON, это 90% проблем.
